# 本轮验证记录

日期：2026-09-10。已从静态美术阶段推进到本地可玩第一版；未部署或连接 VPS。

| 检查 | 结果 |
| --- | --- |
| 可复用美术 | 28 个 SVG，合计 17,720 字节，未压缩 |
| 音频 | 6 个 MP3，共约 314 KB；新增 3 秒满蛋庆祝音效 61 KB，背景音乐为 24 秒、64 kbps 的 193 KB 循环片段，首次下蛋后才加载 |
| SVG 解析 | 28 个游戏资源与 3 个设计合成稿均可按 XML 解析 |
| 资源结构 | 所有 SVG 都有明确的 width、height、viewBox；母鸡帧统一 256 × 256，小鸡帧统一 128 × 128 |
| 独立性 | 无脚本、事件处理器、外部图片、外部字体引用、滤镜或内嵌动画；28 个游戏资源均无文字节点 |
| 栅格化 | 28 个资源均可渲染；27 个透明资源有可见图形，透明画布四边均无非透明像素接触 |
| 单元测试 | `npm run test`：11 项通过，覆盖 5.46 秒慢孵化、幼鸡成长、成年鸡同步批量下蛋与位置预留、空位少于母鸡时的部分批次、无固定 96 颗上限、8 秒／16 秒空闲离场及越界回收、达到动态满屏阈值后的 10 秒冷却离场、成年鸡与小鸡的随机外游、蛋位间距和画布重映射，以及母鸡脚边的首颗蛋 |
| 生产构建 | `npm run build`：TypeScript 检查和 Vite 构建通过；主脚本 gzip 327.17 KB，无 source map；完整静态包约 1.6 MB |
| 冷缓存传输估算 | 首屏 HTML、脚本、样式和 23 个 SVG：gzip 334 KB、Brotli 269 KB；首次下蛋加载背景音乐和下蛋声后：gzip 525 KB、Brotli 458 KB；一局中所有音效都首次加载后：gzip 641 KB、Brotli 573 KB |
| 奖励音效交付 | `audio/full-egg-fanfare.mp3` 在本地开发服务器返回 `200` 与 `audio/mpeg`；时长 3.00 秒、160 kbps、61,221 字节 |
| 依赖审计 | `npm audit`：0 个已知漏洞 |
| 全屏竖屏 | Chrome 390 × 844 中确认 `game-shell`/canvas 均为 390 × 844，滚动尺寸与视口一致；蛋出现在母鸡脚边，暂停页包含标题与操作提示 |
| 全屏横屏 | Chrome 844 × 390 中确认 `game-shell`/canvas 均为 844 × 390，滚动尺寸与视口一致；已有蛋随旋转重映射，本局分数保持 |
| 浏览器日志 | 桌面与手机检查后无 error 或 warning |
| 静态预览 | 使用 `vite preview` 打开 `dist/`，确认启动、下蛋、孵化与浏览器日志均正常 |
| 美术总览 | 1600 × 1440 PNG，已查看母鸡姿态、小鸡姿态、蛋、图标与配色 |

PNG 由独立 SVG 渲染，用于方便查看。画稿中的阴影接地位置已在审阅后调整。三个预览均是静态图，不含可交互按钮；示例分数不代表测试游戏数据。

本轮已创建游戏 JS/TS/HTML、依赖清单与静态构建配置；未创建或修改 VPS 配置。未进行真机触摸、实际扬声器听音、性能剖析、离线刷新或多浏览器兼容性测试。

下一轮的验收范围见 [架构文档](architecture.md) 中“实现阶段验收标准”。
